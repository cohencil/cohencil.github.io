stages:
  - build

variables:
  NODE_VERSION: "20.17.0"

cache:
  paths:
    - node_modules/

default:
  image: node:${NODE_VERSION}
  before_script:
    - echo "USING NODE ${NODE_VERSION}"
    - npm ci

pages:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - public
